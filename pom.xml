<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.poseidon</groupId>
    <artifactId>pcs-trading-app</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Poseidon Capital Solutions - Trading App</name>
    <description>Application de gestion des offres, courbes, transactions et utilisateurs pour Poseidon Capital
        Solutions (Projet 7 - OpenClassrooms)
    </description>

    <!--
  Mise à jour vers Spring Boot 3.5.6 (release du 18 septembre 2025)
  Raison : corriger les vulnérabilités transitives signalées par Maven/IntelliJ
  et bénéficier des dernières optimisations et correctifs de sécurité.
  Compatible Java 17 et Jakarta EE 10.

  Source de référence :
  https://mvnrepository.com/artifact/org.springframework.boot/spring-boot-starter-parent
-->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.6</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <properties>
        <!-- Définit l'encodage global pour les fichiers source (Java) lus par Maven -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <!-- Définit l'encodage des rapports générés par Maven (Surefire, JaCoCo, etc.) -->
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version> <!-- set the version of java -->
        <jacoco.version>0.8.12</jacoco.version> <!-- set the version of jacoco -->
        <lombok.version>1.18.30</lombok.version> <!-- set the version of lombok -->
        <mapstruct.version>1.6.3</mapstruct.version> <!-- set the version of mapstruct -->
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <version>3.1.0</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
            <version>3.1.0</version>
        </dependency>
        <!-- Dépendances Spring Boot -->
        <!-- Provides the necessary components to build web applications using Spring MVC.
        Includes an embedded Tomcat server, Jackson for JSON processing, and a logging framework. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
            <version>3.1.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
            <version>3.1.0</version>
        </dependency>
        <!-- Dépendance MySQL -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- Starter Spring Boot pour activer la validation des DTOs et entités avec les annotations @Valid, @NotBlank, @Email, @Size, etc.
     Ce starter inclut automatiquement les dépendances de Jakarta Validation et le moteur Hibernate Validator. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!-- Librairie java_dotenv pour charger les variables d'environnement depuis un fichier .env.test -->
        <dependency>
            <groupId>io.github.cdimascio</groupId>
            <artifactId>java-dotenv</artifactId>
            <version>5.2.2</version>
        </dependency>
        <!-- =============================================================== -->
        <!-- === MapStruct : Moteur de génération automatique de mappers === -->
        <!-- =============================================================== -->
        <!-- MapStruct simplifie la conversion entre les entités JPA et les DTOs.
             Il génère automatiquement le code de mapping (ex. RuleMapperImpl.java) à la compilation, à partir d'interfaces annotées avec @Mapper.
             Pas de code "convertToDto" / "convertToEntity" manuel -->
        <!-- Librairie principale (runtime) :
        Fournit les annotations (@Mapper, @Mapping, etc.) utilisées dans le code source -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
            <version>${mapstruct.version}</version>
        </dependency>
        <!-- Processeur d'annotations (compiler time only) :
        C'est lui qui, pendant la compilation, analyse les interfaces @Mapper et génère automatiquement les classes d'implémentation (ex : RuleMapperImpl).
        Marqué en <scope>provided</scope> pour ne pas être inclus dans le JAR final, car il n'est nécessaire qu'au moment de la compilation. -->
        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct-processor</artifactId>
            <version>${mapstruct.version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
        </dependency>
        <!-- Lombok : annotations de génération de code à la compilation (getters, setters, etc.)
     Scope 'provided' : Lombok est utilisé uniquement à la compilation,
     il n'est pas inclus dans le JAR final, car les méthodes générées sont déjà présentes -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${lombok.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Enables building, packaging, and running the application with Maven.
              Supports commands like 'mvn spring-boot:run' and 'mvn package'.-->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <!-- Plugin pour forcer l'encodage UTF-8 lors de la copie des ressources (application.properties, templates, etc.)
                  Évite les erreurs “MalformedInputException” dues aux caractères accentués -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <!-- Maven Compiler Plugin + Lombok support -->
            <!-- Configure the Maven Compiler Plugin to enable Lombok annotation processing,
  so that Lombok can generate code (getters, setters, constructors, etc.) during compilation -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8
                    </encoding><!-- Empêche les erreurs liées aux caractères accentués dans les classes Java -->
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${mapstruct.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!-- Maven Surefire Plugin configuration for running unit tests (*Test.java) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <includes>
                        <include>**/*Test.java</include>
                    </includes>
                    <excludes>
                        <exclude>**/*IT.java</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!-- Maven Failsafe Plugin configuration for running integration tests (*IT.java) -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <id>integration-test</id>
                        <phase>integration-test</phase>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                        <configuration>
                            <includes>
                                <include>**/*IT.java</include>
                            </includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Jacoco for code coverage during tests.
             Generates an HTML report at: target/site/jacoco/index.html -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jacoco-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <developers>
        <developer>
            <id>sarar</id>
            <name>Sarar Hammar</name>
            <organization>OpenClassrooms</organization>
            <organizationUrl>https://openclassrooms.com</organizationUrl>
            <roles>
                <role>Développeuse Back-end</role>
            </roles>
            <timezone>Europe/Paris</timezone>
        </developer>
    </developers>
    <scm>
        <connection>scm:git:https://github.com/suebdh/PCS_TradingApp.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/suebdh/PCS_TradingApp.git</developerConnection>
        <!-- Lien de consultation du dépôt sur GitHub -->
        <url>https://github.com/suebdh/PCS_TradingApp</url>
    </scm>

</project>